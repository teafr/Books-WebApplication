import React from 'react'

interface Props { 
    bookName: string,
    author: string,
    imageUrl: string
};

const Book = (props : Props) => {
  return (
    <div>
      
    </div>
  )
}

export default Book
